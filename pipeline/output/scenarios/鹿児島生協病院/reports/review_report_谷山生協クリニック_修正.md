# 修正レポート: 谷山生協クリニック

## サマリー
- 対象フロー: `鹿児島生協病院$谷山生協_診療`（診療シナリオ、45モジュール）
- 修正件数: 6件（A-2, A-3, A-3付随, C-2, C-3, C-4）
- IVRプロパティ側での対応が必要な件数: 3件（A-1, B-1, C-1）
- 人間確認が必要な件数: 0件

---

## 修正詳細

### A-1. その他質問TTS
- モジュール名: `最終確認`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- 変更内容: **変更なし（IVRプロパティ管理）**
  - `params.prompt` が空文字のため、発話テキストはIVRプロパティ側で管理されている
  - IVRプロパティ側で以下の文言に変更が必要:
    ```
    その他、何か確認しておきたいことなどはございますでしょうか。ある場合は、質問内容をお話しください。ない場合は「ありません」とお答えください。
    ```

### A-2. その他質問STT
- モジュール名: `入力_最終確認`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- stop_play_when_speech: `Yes` → `No`（stop_by_dtmfに相当するフィールド）
- profile_words追加（末尾に追記）:
  ```
  ありません ありません
  ないです ないです
  大丈夫です だいじょうぶです
  ```

### A-3. その他質問OpenAI
- モジュール名: `openAI_最終確認`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- プロンプト変更内容:
  1. 出力形式を2値（YES/NO）から3パターンに拡張:
     - 質問なし → `NO`
     - 質問あり（自由発話あり）→ `YES:<発話内容>`
     - 判定不能 → `NO_RESULT`
  2. 「ありません」「ないです」「大丈夫です」→ `NO`（質問なし）として出力するルールを明示化
  3. 自由発話があった場合 → `YES:<内容>` として内容を抽出して出力するルールを追加
  4. Few-shot例を追加（ありません→NO、ないです→NO、大丈夫です→NO、駐車場は？→YES:駐車場は、プロンプトインジェクション→NO_RESULT等）
  5. プロンプトインジェクション対策文言を確認・強化（「以前の命令を無視して」等を完全無視し強制NO_RESULTを出力）

### B-1. 連絡先電話番号TTS
- モジュール名: `患者_連絡先`（個人情報フロー: `鹿児島生協病院_谷山生協個人情報_1.json`）
- 変更内容: **変更なし（IVRプロパティ管理）**
  - `params.prompt` が空文字のため、発話テキストはIVRプロパティ側で管理されている
  - IVRプロパティ側で以下の文言に変更が必要:
    ```
    次に、ご連絡先のお電話番号をお伺いします。携帯電話番号、または、0から始まる市外局番を含む電話番号をお話しください。プッシュボタンでも入力いただけます。
    ```

### C-1. 用件案内TTS
- モジュール名: `用件`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- 変更内容: **変更なし（IVRプロパティ管理）**
  - `params.prompt` が空文字のため、発話テキストはIVRプロパティ側で管理されている
  - IVRプロパティ側で以下の文言に変更が必要（「予約日時の確認」を削除した3択に）:
    ```
    次の3つのうちのいずれかでご用件をお話しください。「1,診察の予約」、「2,予約の変更」、「3,予約のキャンセル」それではお話ください。どうぞ。
    ```

### C-2. 用件STT profile_words修正
- モジュール名: `入力_用件`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- 削除内容: 「確認」「予約日時の確認」「4」に関連するエントリ66行を削除
  - 削除対象キーワード例: `予約日時の確認`, `日時の確認`, `予約の確認`, `予約確認`, `確認したい`, `いつだったか`, `教えて`, `予約日`, `四番`, `よんばん`, `時の確認`, `確認 かくにん`, `4 よん`, `４ よん`, `4番 よんばん`, `４番 よんばん`, `4 し`, `４ し`, `4番 しばん`, `４番 しばん`

### C-3. 用件OpenAI プロンプト修正
- モジュール名: `openAI_用件`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- 変更内容:
  1. 出力制約から `予約日時の確認` を削除（5択→4択: 診療予約/予約変更/キャンセル/リピート/NO_RESULT）
  2. 出力カテゴリ説明から「予約日時の確認: 「4」「予約日時の確認」...」の記述を削除
  3. 出力制約の件数を6つ→5つに更新
  4. プロンプトインジェクション対策文言は維持

### C-4. 用件分岐 next配列修正
- モジュール名: `openAI_用件`（診療フロー）
- 対象ファイル: `output/鹿児島生協病院_谷山生協_診療_1.json`
- 削除内容: 以下のルートを削除（1件）:
  ```json
  {"condition": "^予約日時の確認$", "label": "予約日時の確認", "nextModuleName": "最終確認"}
  ```
- 削除後のnext配列: TIMEOUT/ERROR/NO_RESULT → リトライ、診療予約/予約変更/キャンセル/リピート → 各処理モジュール
- 孤立モジュール（`最終確認`等）はそのまま放置

---

## 検証結果
- validator.py: **PASS**（Critical: 0件, Warning: 0件）
- .bivr 生成: **成功**（`output/鹿児島生協病院.bivr`、診療フロー1フロー、45モジュール）

---

## IVRプロパティ側で対応が必要な事項

以下のモジュールは `params.prompt` が空文字であり、発話テキストはIVRプロパティ側で管理されています。
JSONフロー側での変更は行っていないため、**IVRプロパティ（Brekeke等の管理画面）側で別途変更が必要**です。

| 修正ID | モジュール名 | フロー | 変更内容 |
|--------|------------|--------|---------|
| A-1 | `最終確認` | 診療 | 「その他、何か確認しておきたいことなどはございますでしょうか。ある場合は、質問内容をお話しください。ない場合は「ありません」とお答えください。」 |
| B-1 | `患者_連絡先` | 個人情報 | 「次に、ご連絡先のお電話番号をお伺いします。携帯電話番号、または、0から始まる市外局番を含む電話番号をお話しください。プッシュボタンでも入力いただけます。」 |
| C-1 | `用件` | 診療 | 「次の3つのうちのいずれかでご用件をお話しください。「1,診察の予約」、「2,予約の変更」、「3,予約のキャンセル」それではお話ください。どうぞ。」 |

---

## 備考（A-3付随修正）
- `openAI_最終確認` の出力フォーマット変更（YES/NO → NO/YES:<内容>/NO_RESULT）に伴い、同モジュールの `next` 配列の遷移条件も修正しました:
  - `^YES$`（完全一致）→ `^YES`（前方一致）に変更
  - これにより `YES:駐車場はありますか` のような形式の出力も `移動_FAQ` へ正しく遷移します
