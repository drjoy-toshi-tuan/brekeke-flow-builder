# 確認レポート: イーストメディカルクリニック — 健診

> 生成日: 2026/04/27
> 生成元: @director エージェント
> 出力: `docs/designs/設計書_イーストメディカル_健診.yaml`

---

## 1. 材料の解析結果

### 受領した材料

| # | 材料種別 | ファイル名/内容 | 概要 |
|---|---|---|---|
| 1 | A（Markdown：移管ノート） | `docs/migration/gen2_イーストメディカル_健診.md`（37行） | 移管方針サマリ・施設固有特殊ルール・director が見落としやすいポイントを箇条書きで整理。元資料へのポインタを提供 |
| 2 | A（Markdown：Customer Doc） | `docs/reference/customer_docs/【健診1】：イーストメディカル.md`（2337行） | Gen2 完全資料。`# yaml`（末尾、確定版フロー）/ `# 本番＿function`（17 フィールド）/ `# 本番＿プロンプト_20260206`（救済語・smsFlag/status 制御・電話番号判定）等を含む。優先順位: yaml=主、function=スキーマ、本番＿プロンプト=補助 |

### 判定した作業種別

- **作業種別**: Gen2→Gen3 移管
- **施設名**: イーストメディカルクリニック
- **シナリオ名**: 健診
- **対象フロー**: `イーストメディカル$健診`
- **判定根拠**: 入力資料が `docs/migration/gen2_*.md` 形式（Gen2→Gen3 移管ノート）で、`# 元資料: docs/reference/customer_docs/【健診1】：イーストメディカル.md` の指定がある。元資料に Gen2 フロー定義（yaml）+ Gen2 function スキーマ + Gen2 プロンプトが揃っており、第3世代へのスキーマ変換が必要。

### 抽出した要件サマリー

- **冒頭**: 1 種類の冒頭アナウンス（"お電話ありがとうございます。イーストメディカルクリニックの、健診専用AI電話です。"）
- **個人情報聴取**: 受診者名（カタカナ変換、復唱禁止）→ 生年月日（リトライ2、和暦→西暦変換）→ 連絡先電話番号（incoming_phone_type で 3 分岐）
- **連絡先電話番号**:
  - MOBILE → 着信番号読み上げ Yes/No 確認（echo_back あり）
  - FIXED → 「日中、ご連絡のとれる携帯電話番号をお話しください。ない場合は『ありません』」
  - OTHER → 「日中、ご連絡のとれる電話番号…SMS受信可能な携帯または市外局番」
  - 3 回失敗時の二段階フォールバック（anonymous → END_聴取失敗_匿名 / 非匿名 → 着信番号適用 → 用件確認）
- **用件確認**: 4 分岐（予約 / 変更 / キャンセル / 問い合わせ）。「その他」「確認」関連語は問い合わせへ catch-all
- **予約ルート**: 健保組合等名称 → 受診希望コース（30 件弱リスト + 救済語）→ 予約希望時期（フリーワード）→ 追加質問 → END_予約等（status=1, smsFlag=1）
- **変更ルート**: 現在の予約日（YYYY-MM-DD 形式）→ 変更内容 → 追加質問 → END_予約等（status=1, smsFlag=1）
- **キャンセルルート**: 現在の予約日 → 追加質問 → END_キャンセル（status=1, smsFlag=2）
- **問い合わせルート**: 問い合わせ内容（AI 回答せず保存のみ）→ 追加質問 → END_予約等（status=1, smsFlag=1）
- **終話**: 4 種類（END_予約等 / END_キャンセル / END_聴取失敗_匿名 / END_聴取失敗）+ Gen3 標準（END_非通知 / END_時間外）
- **acceptance_times 不使用**（24時間受付、移管ノート §移管方針）
- **転送なし**（全自動応答完結）
- **RAG/FAQ なし**（Gen2 yaml に未記載、問い合わせ内容はそのまま question 保存）
- **救済語**（OpenAI prompt 必須）:
  - 用件: 検針/返品/返信/天候診断/返金控寸断 → 健診 / 人間ロック/ドック/ロック/人間独 → 人間ドック / 薬/お薬 → 予約 / 取り直し/別の/変えたい → 変更 / やめる/取り消し → キャンセル
  - 数字補正: 4/４/Sea/しー/し/よん/よー → 4 / Go/ご/ごお/ごー/いつ → 5 等

---

## 2. 確認項目

### BLOCKER（解消必須 — この情報がないと指示が不完全）

> ✅ BLOCKER: なし。設計書はそのまま @generator に実行を指示できる状態。

### NON-BLOCKER（仮値で設定済み — 確認後に差し替え推奨）

| # | 分類 | 対象 | 仮設定値 | 確認内容 |
|---|---|---|---|---|
| N-1 | MISSING | `office_id` | `TODO_要確認` | Dr.JOY 管理画面で確認して設定（CLAUDE.md ルール: 常に NON-BLOCKER） |
| N-2 | MISSING | IVR 着信電話番号（ARS番号、本番） | （設計書非掲載） | Brekeke 設定時に入力。設計書には記載しない |
| N-3 | DECISION | `group_name` | `イーストメディカル`（カナ8文字、URL長安全） | Brekeke のグループ名が正式に「イーストメディカルクリニック」等の別名で登録される予定なら差し替え |
| N-4 | DECISION | `use_acceptance_times: false`（24時間受付） | false | Gen2 yaml に Date of Call Classifier 不要の指定あり。AI 電話を夜間も稼働させる運用で問題ないか要確認 |
| N-5 | DECISION | 電話番号のインライン実装（subflow 不採用） | hearing + ContextMatchRouter | Gen2 の MOBILE/FIXED/OTHER 3 種 TTS を保持するためサブフロー化を回避。標準サブフロー統一ポリシーがある場合は再検討（director 判断詳細はセクション 4） |
| N-6 | DECISION | `[電話番号_携帯確認]` の echo_back=true で `<speak type="telephone" breakc="300ms">{}</speak>` を含む TTS が読み上げられる挙動 | echo_back: true（Gen2 仕様維持） | Brekeke の TTS が SSML を正しく解釈するか確認（標準サブフローと同等の挙動になるはず） |
| N-7 | DECISION | `retry_failure: skip` 経由で「次ブロック=匿名判定_内部判定」へ遷移するか | skip + next: 匿名判定_内部判定 | scaffold が連絡先電話番号_固定確認 / その他確認 の retry exceeded を匿名判定_内部判定 に正しく配線するか検証 |
| N-8 | DECISION | `context_match_router.reference_module: "incoming-classifier"` の解決可否 | incoming-classifier 直接参照 | Brekeke システム変数（incoming-classifier モジュール出力）を context_match_router の reference_module に直接指定できるか scaffold で検証 |
| N-9 | DECISION | context_match_router の match 値（"MOBILE" / "FIXED" / "default" / "非通知"）の妥当性 | Gen2 仕様に準拠 | incoming-classifier モジュールの実出力値と一致するか scaffold/qa_validator で検証 |
| N-10 | MISSING | 受診希望コースリスト 30 件弱（Gen2 §3） | profile_words に全件登録済み | OpenAI prompt のリスト記載・リスト外案内は prompter で実装する前提 |
| N-11 | DECISION | 生年月日サブフローの retry 上限 2 への上書き | サブフロー bivr 完全コピー後、IVRプロパティで Retry を 2 に上書き | Gen2 §1-10 準拠。手動オーバーライド運用の確認 |
| N-12 | DECISION | Gen2 「複数名予約 catch-all（企業/団体予約は HP からのみ）」の取り扱い | 未実装（移管ノート §施設固有で割愛推奨） | 要件に含めるか確認 |
| N-13 | MISSING | SMS 送信文面（smsFlag=1: 予約/変更/問い合わせ完了 / smsFlag=2: キャンセル完了） | フローJSON 側は smsFlag 値のみ指定 | Dr.JOY 管理画面で各 SMS テンプレート文面を設定 |
| N-14 | DECISION | Gen2 `<break time="10000ms"/>`（10秒無音）の削除 | Gen3 では削除（SSML 禁止ルール） | 終話直前の間が短くなる。施設側で運用上問題ないか確認 |

---

## 3. Dr.JOY側設定事項

設計書に記載されているが、フローJSONではなく **Dr.JOYシステム側で設定が必要な項目** を以下にまとめる。

| # | 設定項目 | 設計書記載値 | 備考 |
|---|---|---|---|
| 1 | SMS送信文面（smsFlag=1） | フローJSON 側は smsFlag=1 指定のみ | 予約/変更/問い合わせ完了用 SMS テンプレート。Dr.JOY 管理画面から設定 |
| 2 | SMS送信文面（smsFlag=2） | フローJSON 側は smsFlag=2 指定のみ | キャンセル完了用 SMS テンプレート。Dr.JOY 管理画面から設定 |
| 3 | 認証設定（優先順位） | 設計書には記載なし | Gen2 仕様で認証フローなし。Dr.JOY 側で本フロー専用の認証は不要 |
| 4 | 画面表示設定 | フィールド表示ルール | 各 context_field（受診者名・生年月日・連絡先電話番号・健保組合等名称・受診希望コース・予約希望時期・現在の予約日・変更内容・問い合わせ内容・追加の質問・状態）の Dr.JOY 表示順・ラベル等は Dr.JOY 管理画面から調整 |
| 5 | office_id | `TODO_要確認` | Dr.JOY 管理画面で確認 |

---

## 4. augment ブロック使用状況

> ✅ augment ブロック未使用。9 ブロック型（opening / announcement / hearing / subflow / context_match_router / script / date_of_call_classifier / call_transfer / termination）のみで構成。

---

## 5. directorの判断ログ

| # | 判断箇所 | 選択肢 | 採用した判断 | 根拠 |
|---|---|---|---|---|
| 1 | 電話番号聴取の実装方式 | A) 標準サブフロー (`イーストメディカル$電話番号聴取`) を完全コピー / B) インライン hearing + context_match_router で実装 | **B（インライン）** | Gen2 yaml は MOBILE/FIXED/OTHER で **3 種類の異なる TTS** を持ち、特に FIXED は「ありません」許容と携帯番号限定指定（"日中、ご連絡のとれる携帯電話番号…ない場合は「ありません」"）が標準サブフローと不一致。標準サブフローを完全コピーするとこの差異が失われ Gen2 仕様の劣化となる。さらに 3 回失敗時の二段階フォールバック（anonymous vs 非匿名）はメインフロー側でしか実装できないため、インライン化が自然 |
| 2 | 用件分岐の実装パターン | A) Pattern A（OpenAI 直後の enum conditions で分岐）/ B) Pattern B（OpenAI → ContextMatchRouter で分岐） | **A（Pattern A）** | 移管ノートに「ContextMatchRouter で 4 分岐」と書かれているが、複数 context 組み合わせ判定不要のため Pattern A で十分。OpenAI 出力 `{予約, 変更, キャンセル, 問い合わせ}` に対して conditions を直接定義 |
| 3 | サブフロー構成 | A) PatientName / DateOfBirth / PhoneNumber の 3 本 / B) PatientName / DateOfBirth の 2 本 + PhoneNumber インライン | **B（2 本 + インライン）** | 判断 1 と連動。電話番号は Gen2 仕様の保持を優先しインライン化したため、サブフローは PatientName と DateOfBirth の 2 本のみ |
| 4 | 診察券番号サブフロー | A) 含める / B) 含めない | **B（含めない）** | 移管ノート §移管方針：「診察券番号サブフローは 使わない（function スキーマには medicalCardNumber があるが、シナリオでは聴取しない）」 |
| 5 | function スキーマ採用 | A) 本番版（17 フィールド）/ B) デモ版（19 フィールド：DesiredreservationDate/Health_insurance_subsidy/endpoint/Incoming_phone_type 含む） | **A（本番版）** | 移管ノート §施設固有：「endpoint / Health_insurance_subsidy / DesiredreservationDate は本番 function スキーマに存在しない（デモ版のみ）。本番 function を採用」 |
| 6 | acceptance_times | A) 配置（時間外チェックあり）/ B) 不使用（24時間受付） | **B（不使用）** | 移管ノート §移管方針：「Date of Call Classifier: 不要。営業時間外分岐の指定なし」 |
| 7 | RAG/FAQ サブフロー | A) パターン1 / 2 / 3 のいずれかで配置 / B) なし | **B（なし）** | Gen2 yaml に RAG/FAQ 対応の記述なし。問い合わせ内容はそのまま question に保存して終話する（CLAUDE.md Rule 16: Gen2→Gen3 デフォルトはパターン3 だが、元資料に該当機能なしのため none を採用） |
| 8 | 終話 status 値（聴取失敗系） | A) status=3（テンプレデフォルト）/ B) status=2（Gen2 値） | **B（status=2）** | Gen2 §status「代表電話番号を案内した場合は 2」+ Gen2 yaml 終了箱「END_聴取失敗 status=2」「END_聴取失敗_匿名 status=2」を厳密準拠 |
| 9 | Gen2 `<break time="10000ms"/>`（10秒無音）の取り扱い | A) 維持 / B) 削除 | **B（削除）** | CLAUDE.md「SSML タグ（`<speak>`, `<break>`, `<say-as>` 等）は使用禁止」+ 移管ノート §director が見落としやすい：「Gen3 では削除し、必要なら無音区間は IVR 側で対応」。`<speak type="telephone" breakc="300ms">` のみ電話番号復唱で例外保持 |
| 10 | medicalCardNumber / clinicalDepartment context フィールド追加可否 | A) 含める（Gen2 function 必須）/ B) 含めない（シナリオで聴取しない） | **B（含めない）** | Gen2 function 必須だが、Gen3 ではシナリオで聴取しないフィールドを context_fields に含めない方針。Dr.JOY 表示が必要なら別途確認 |
| 11 | 用件 enum「無回答」の扱い | A) classification rangeValues に含める / B) 除外（Gen3 で使わない） | **A（含める）** | Gen2 function enum に「無回答」あり。ContextMatchRouter での分岐値ではないが、context 値として正規化（saveContextModel2DB） |
| 12 | smsFlag 分岐の実装 | A) ContextMatchRouter / Script で分岐 / B) 各 saveCompletionFlag2db で個別指定 | **B（個別指定）** | smsFlag は終話チェーンごとに固定値（END_予約等=1 / END_キャンセル=2 / 他=0/-1）。ContextMatchRouter による分岐は不要。`sms_flag_routing.enabled: false` |

---

## 6. 次のアクション

1. ✅ 設計書はそのまま実行可能（BLOCKER なし）
2. NON-BLOCKER 項目（特に N-3 group_name / N-4 acceptance_times / N-5 電話番号インライン化 / N-7〜N-9 incoming-classifier 直接参照）について、Brekeke 実装上の検証または運用判断を取り次第、設計書を必要に応じて更新する
3. @generator → @prompter → @reviewer → @fixer → @tester のパイプラインに `docs/designs/設計書_イーストメディカル_健診.yaml` を投入する
4. Dr.JOY 側設定事項（SMS 文面、office_id 等）はフロー稼働前に Dr.JOY 管理画面で設定する
5. 生年月日サブフローのリトライ上限 2 への上書き（Gen2 §1-10 準拠）は、サブフロー bivr 完全コピー後に IVRプロパティで Retry の最大回数を 2 に変更する運用が必要（gen_properties.py の挙動を確認）
