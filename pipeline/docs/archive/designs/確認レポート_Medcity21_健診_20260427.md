# 確認レポート: 大阪公立大学医学部附属病院 先端予防医療部附属クリニック MedCity21 — 健診

> 生成日: 2026/04/27
> 生成元: @director エージェント

---

## 1. 材料の解析結果

### 受領した材料

| # | 材料種別 | ファイル名/内容 | 概要 |
|---|---|---|---|
| 1 | A (Markdown 移管ノート) | `docs/migration/gen2_Medcity21_健診.md` | Gen2→Gen3 移管方針サマリ。group_name 短縮形指定、Pattern A 採択、サブフロー分割、施設固有特殊ルール（救済語・数字正規化・終話テンプレ選択順）を明記 |
| 2 | A (Markdown 元資料) | `docs/reference/customer_docs/【健診1】：大阪公立大学医学部附属病院Medcity21.md` | Gen2 統合プロンプト本体（1205行）。# プロンプト + # function スキーマ + # yaml 対話フロー定義の三層統合型 |

### 判定した作業種別

- **作業種別**: Gen2→Gen3 移管
- **施設名**: 大阪公立大学医学部附属病院 先端予防医療部附属クリニック MedCity21
- **シナリオ名**: 健診
- **対象フロー**: `Medcity21$健診_20260427`
- **判定根拠**: 移管ノートに「Gen2→Gen3 移管」と明記。元資料は Gen2 形式（Assistant プロンプト + function schema + yaml の三層統合型）

### 抽出した要件サマリー

- **5 用件分岐**: 予約 / 日程変更 / キャンセル / オプション追加変更 / その他お問い合わせ + 聴取失敗フォールバック（→問合せに合流）
- **予約フロー**: 受診歴確認 → (履歴あり) コース内容確認 → (踏襲) オプション追加 OR (再選択) 保険組合名 → 希望コース → オプション追加 → 希望時期 → 共通
- **変更フロー**: 条件確認（1週間以内？はい→代表案内）→ 現在予約日 → 希望時期 → 共通
- **キャンセルフロー**: 条件確認（2週間以内？はい→代表案内）→ 現在予約日 → 振替希望（はい→変更フローに合流） → 共通
- **オプションフロー**: 現在予約日 → 内容聴取 → 共通
- **問合せフロー**: 内容聴取 → キーワード判定（健診結果系→代表案内）→ RAG → 返信希望（希望しない→END_返信不要 / 希望する→共通）
- **共通**: 氏名サブフロー → 生年月日サブフロー → 電話番号サブフロー → ラスト質問 → RAG → 終話分岐カスケード
- **終話分岐**: 4 段 ContextMatchRouter（用件→振替→返信→電話種別）で 5 終話パターン（END_共通_携帯 / 携帯以外 / キャンセル / 返信不要 / 代表案内）に振分
- **異常系**: 非通知 / 聴取失敗 はデフォルトテンプレートのまま採用（受付時間外は use_acceptance_times=false で非適用）
- **コース10種・オプション3種・保険組合2種**: 移管ノートに従い OpenAI プロンプトに enum + aliases を渡す
- **施設固有救済語**: 健診/人間ドック/予約/変更/キャンセル/肯定/否定 の言い換え辞書、数字正規化（し→4, ご→5, GO→5, 救急→99）
- **復唱**: 生年月日のみ1回、非通知時の電話番号確認1回（いずれもサブフロー内で完結）
- **RAG パターン3**: 問合せ_内容聴取後 + ラスト質問後の二重配置（is_question ガード付き）

---

## 2. 確認項目

### BLOCKER（解消必須）

> BLOCKER: なし

設計書はそのまま @generator に渡せる状態です。`TODO_要確認` は `office_id` のみで、これは Dr.JOY 側で確認すれば足りる NON-BLOCKER 扱いです。

### NON-BLOCKER（仮値で設定済み — 確認後に差し替え推奨）

| # | 分類 | 対象 | 仮設定値 | 確認内容 |
|---|---|---|---|---|
| N-1 | MISSING | basic_info.office_id | `"TODO_要確認"` | Dr.JOY 管理画面で施設IDを確認。フローJSON生成には不要だが、Dr.JOY側設定で必要 |
| N-2 | DECISION | 用件確認_失敗_用件書換 (augment) | placeholder Script | scaffold が placeholder を生成するため、稼働前に Brekeke IVR UI で saveContext2DB（contextName=classification, value=問合せ）に手動差し替えが必要 |
| N-3 | DECISION | 問合せ_キーワード判定 (CMR 正規表現) | `.*健診の結果.*` 等 | ContextMatchRouter の正規表現マッチング動作を実機で確認。期待挙動: reason 内に「健診の結果／人間ドックの結果／健康診断の結果」が含まれていれば END_代表案内 へ |
| N-4 | DECISION | 終話TTS（共通_携帯／携帯以外）の用件名 | 汎用「お申し込み」表現 | Gen2 は「（予約/変更/オプションの追加・変更/問合せ）のお申し込み」と classification 名を埋め込み。Gen3 では汎用表現に簡素化。顧客が用件名込みを希望する場合は 4 用件 × 2 = 8 種の TTS に分割が必要 |
| N-5 | DECISION | 電話番号聴取サブフローの非通知聴取失敗遷移 | サブフロー内 self-handling | customer_doc では END_代表案内（status=2, smsFlag=0）。サブフロー return 型のため、非通知聴取失敗パスをサブフロー内で完結させるか、親フローへシグナルするかを Brekeke IVR UI で手動配線確認 |
| N-6 | MISSING | RAG (FAQ参照V2) の FAQ マスタ登録 | 未登録 | Dr.JOY 側 FAQ リストに駐車場/アクセス/受付時間/コース料金/所要時間/持ち物 等の施設系トピックを事前登録。NO HIT 時は固定許可文「ご質問いただいた内容はAI電話ではご対応できませんので、折り返しご連絡時にご確認ください。」 |

---

## 3. Dr.JOY側設定事項

| # | 設定項目 | 設計書記載値 | 備考 |
|---|---|---|---|
| 1 | office_id | TODO_要確認 | Dr.JOY 管理画面で確認。フローJSON生成では不要 |
| 2 | SMS 送信文面（smsFlag=1） | 設計書には記載なし | Dr.JOY 側で設定。携帯ルート全件で smsFlag=1（予約/変更/オプション/問合せ希望する 共通） |
| 3 | SMS 送信文面（smsFlag=2） | 設計書には記載なし | Dr.JOY 側で設定。キャンセル専用 |
| 4 | 認証設定（優先順位） | 設計書には記載なし | 生年月日・電話番号の認証優先順位は Dr.JOY 側で設定 |
| 5 | RAG / FAQ リスト | 元資料に「外部RAG」と記載 | Dr.JOY 側 FAQ マスタに施設系トピックを事前登録（駐車場/アクセス/受付時間/コース料金/所要時間/持ち物 等） |
| 6 | 営業時間スケジュール | use_acceptance_times=false で非設定 | フロー側では時間外分岐なし。代表案内 TTS で「月〜土 9:00〜16:30」を発話案内のみ |
| 7 | 画面表示設定（確認画面表示 ○/×） | 設計書には記載なし | Dr.JOY 管理画面から設定 |

---

## 4. augment ブロック使用状況

| # | step 名 | augment_pattern | augment_purpose | 推奨アクション |
|---|---|---|---|---|
| 1 | 用件確認_失敗_用件書換 | director_handled | 用件確認の聴取失敗フォールバック時、classification を「問合せ」に上書きしてから問合せフローへ合流。Gen2 customer_doc 仕様の「聴取失敗フォールバック → classification=問合せ書換 → E フロー合流」を再現するため、saveContext2DB を 1 個追加して classification="問合せ" を明示書込みする実装が必要 | scaffold は placeholder Script を生成するため、稼働前に Brekeke IVR UI で **saveContext2DB（contextName=classification, value=問合せ）** に手動差し替え。次回同様パターン（context 上書きが必要な聴取失敗フォールバック）が出てきた場合、専用の `context_overwrite` ブロック型として scaffold_generator に追加することを検討 |

augment は 1 箇所のみ。それ以外は既存 9 ブロック型（opening / announcement / hearing / subflow / context_match_router / termination）で表現できているため、augment 多用による設計の不安定化はなし。

---

## 5. directorの判断ログ

| # | 判断箇所 | 選択肢 | 採用した判断 | 根拠 |
|---|---|---|---|---|
| 1 | use_acceptance_times | true / false | **false** | 移管ノート #16「Date of Call Classifier 不要（受付時間外分岐の指示なし、endpoint=時間外 enum はあるがフロー記載なし → director 判断で省略可）」。元資料も Mermaid フロー図に時間外分岐なし。代表案内 TTS で月〜土 9:00〜16:30 を案内するのみ |
| 2 | 用件確認の OpenAI 分岐パターン | Pattern A（直後分岐） / Pattern B（CMR 後段） | **Pattern A** | 移管ノート #14「Pattern A（OpenAI 直後で classification 分岐）で組む。echo_back や複数 context 組合せの分岐は無いため Pattern B 不要」。OpenAI 出力 5 値（予約/変更/キャンセル/オプション/問合せ）と分岐先が 1:1 対応 |
| 3 | 個人情報聴取の構成 | サブフロー分割 / インライン展開 | **サブフロー分割** | CLAUDE.md Rule 9 + 移管ノート #15。氏名・生年月日・電話番号は必ずサブフロー化。診察券番号は元資料に存在しないため省略 |
| 4 | RAG (FAQ参照V2) の配置 | Pattern 1 / 2 / 3 / none | **Pattern 3** | 元資料が問合せ_内容聴取後（Pattern 1）+ ラスト質問前（Pattern 2）の両方で FAQ V2 を呼ぶ仕様。Gen2→Gen3 デフォルトの Pattern 3 が一致 |
| 5 | 終話分岐の構造 | 単一 Script / CMR カスケード | **CMR 4 段カスケード** | カレンダー計算が不要なため Script は使えない（qa_validator E-13）。3 値（classification, reply, phonetype）+ キャンセル時 reschedule_request 確認の組み合わせで 4 段 CMR で表現 |
| 6 | キャンセル振替希望=希望する 時の終話振分 | classification 物理上書き / 終話分岐_振替で論理振分 | **論理振分（augment 不使用）** | キャンセル時のみ reschedule_request を見て 通常終話側へ流す cascade で対応。classification の物理上書き不要 → augment ブロックを削減できた |
| 7 | 用件確認 default フォールバック | retry_failure: skip 自動 / 明示的 default 条件 + announcement | **明示的 default** | customer_doc に「申し訳ございません、ご回答が確認できませんでした。」のフォールバック TTS が定義されている。announcement → augment(classification 上書き) → 問合せ_内容聴取 で明示配線 |
| 8 | 問合せ内容のキーワード判定 | OpenAI 分岐型 hearing / context_match_router 後段 | **CMR 後段（reason フィールドの正規表現）** | 「健診の結果／人間ドックの結果／健康診断の結果」3 文字列の含有判定なので、CMR の正規表現マッチで十分。OpenAI hearing にすると enum 化で reason の自由テキストが失われる |
| 9 | preferredDate の output_format | datetime / text | **text** | 元資料に「明確日付なら yyyy-MM-dd、不明確ならフリーテキスト」と明記。曖昧表現許容のため text を採用（モジュール選定ガイド 2.4.1 の datetime 制約回避） |
| 10 | reservationDate の output_format | datetime / text | **datetime** | 元資料に「reservationDate: yyyy-MM-dd 00:00:00」と明確指定。曖昧表現は受け付けず datetime 強制 |
| 11 | phone_type と phonetype の関係 | phone_type 4 値そのまま / phonetype 2 値に集約 | **phonetype 2 値に集約** | Gen3 標準の phonetype（携帯/その他）を採用。Gen2 の anonymous/ip_phone/other は「その他」に集約、mobile は「携帯」。saveContextModel2DB は phonetype のみ |
| 12 | status enum 値 | Gen2 の [0,1,2,7] そのまま / Gen3 標準 [1,2,3,6,7] | **Gen3 標準 [1,2,3,6,7]** | CLAUDE.md「第2世代の 0/5 は使用禁止」。status=0 は status=2/3 に置換、status=6 は時間外（use_acceptance_times=false なので未使用だがデフォルト保持） |
| 13 | group_name の長さ | 略称 / 正式名 | **`Medcity21`（短縮形）** | 移管ノート #13。正式名「大阪公立大学医学部附属病院 先端予防医療部附属クリニック MedCity21」は URL エンコード長制限（239バイト以内）に抵触。冒頭 TTS でのみ正式名を使用 |
| 14 | 終話TTSの用件名差込 | 4 用件分割 (8 種 TTS) / 汎用「お申し込み」 | **汎用「お申し込み」** | Gen2 は classification 名を文字列差込していたが、Gen3 scaffold は動的差込しない。4 用件 × 2(携帯/携帯以外) = 8 TTS に分割するか汎用表現にするかは顧客判断 → 汎用化で簡素化、N-4 で再確認 |
| 15 | 終話分岐_振替 の挿入位置 | 終話分岐_用件 の前 / 後 | **後（用件=キャンセル の枝の中）** | 振替希望は キャンセル フローでのみ意味を持つ。用件分岐後に振替判定を入れる cascade のほうが他フローの reschedule_request 評価を回避できて副作用なし |

---

## 6. 次のアクション

設計書 `docs/designs/設計書_Medcity21_健診.yaml` はそのまま実行可能です。

1. **qa_validator 実行**: `python3 scripts/qa_validator.py docs/designs/設計書_Medcity21_健診.yaml --json-report` で機械検証
   - augment 1 箇所（用件確認_失敗_用件書換）が WARNING で検出される想定
   - その他 CRITICAL は出ない想定。出た場合は yaml_auto_fixer.py で機械修正
2. **NON-BLOCKER の確認**: 上記 N-1〜N-6 を Dr.JOY 担当者・顧客と確認
3. **scaffold_generator 実行 → generator パイプライン**: orchestrator 経由でフローJSON生成
4. **稼働前手動配線（重要）**:
   - 用件確認_失敗_用件書換 augment の placeholder Script を Brekeke IVR UI で saveContext2DB に差し替え
   - 電話番号聴取サブフローの非通知聴取失敗パスを END_代表案内 にループバック配線確認
   - RAG パターン3 の二重配置動作確認（問合せ_キーワード判定後 + ラスト質問後）
5. **Dr.JOY 側設定**: office_id 確認、SMS 文面 2 種登録、認証優先順位設定、RAG / FAQ マスタ登録
