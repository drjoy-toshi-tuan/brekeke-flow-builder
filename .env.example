# Google OAuth 2.0 Client ID (Web application). KHÔNG phải secret — an toàn để lộ trong bundle SPA.
# Lấy từ Google Cloud Console (xem README §Google Cloud setup).
# Để trống nếu chỉ muốn test UI ở "chế độ demo" (bỏ qua đăng nhập).
VITE_GOOGLE_CLIENT_ID=

# ── Kho GitHub chứa file YAML (tuỳ chọn) ──
# Mặc định trỏ về repo hiện tại (drjoy-toshi-tuan/brekeke-flow-builder), nhánh main,
# thư mục flows/. Chỉ cần đặt nếu fork sang repo/nhánh/thư mục khác.
# VITE_GITHUB_OWNER=drjoy-toshi-tuan
# VITE_GITHUB_REPO=brekeke-flow-builder
# VITE_FLOWS_BRANCH=main
# VITE_FLOWS_DIR=flows
